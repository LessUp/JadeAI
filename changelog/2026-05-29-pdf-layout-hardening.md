# 2026-05-29 PDF layout hardening

- 抽离 PDF 布局画像、分页策略和基准评估入口，统一预览与导出在模板布局上的共享数据模型与主题 CSS。
- 修复 HTML/PDF 导出对 `themeConfig` 的浅合并问题，避免旧数据或不完整主题配置导致页边距变量缺失、导出与预览不一致。
- 为导出主题默认值和本地 Chromium 解析补充回归测试，继续保留 bundled Chromium 回退路径，同时在找不到系统浏览器时输出显式告警，避免静默下载。
